# Why Elastic for Telco — slides

Edit `slides/public-launch-slides.md`, then:

```bash
npm run build:slides
git add docs/index.html slides/
git commit -m "Update telco slides"
git push
```

**Live:** https://poulsbopete.github.io/telco-demo/
